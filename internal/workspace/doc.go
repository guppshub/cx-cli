/*
Package workspace manages persistent environment contexts for cx-cli.

A workspace consists of a name, provider selection, and provider-specific configurations.
Workspaces are mapped directly to configurations and can be switched, listed, deleted, and renamed.
*/
package workspace
