/*
Package state manages persistent runtime state for cx-cli.

State is stored as JSON and represents machine-managed runtime data (such as active context and connections).

The state package is independent of the config package.
*/
package state
