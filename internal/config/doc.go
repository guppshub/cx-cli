/*
Package config manages persistent configuration for cx-cli.

Configuration is stored as YAML and represents user-managed state.

The package is provider-agnostic and independent of runtime state.
*/
package config
